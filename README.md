# APBD_02
Some new info for the commit
A text for the 5th task...

It was not a fast-forward merge because "main" branch had a new commit.


Some text for the rebase task


1.1) fast-forward when a no commits on main branch were made
1.2) merge commit is created when some changes on either branch were made

2) merge let's user choose which version to keep while rebase is usually just overriding the current version

3) just changed everything in the code, but it also could be made with mergetool or built-in vscode conflict resolver